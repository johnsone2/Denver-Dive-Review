@model Denver_Dive_Review.Models.BarTabModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>BarTabModel</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.BarName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.BarName)
            @Html.ValidationMessageFor(model => model.BarName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.TabAmount)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TabAmount)
            @Html.ValidationMessageFor(model => model.TabAmount)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
